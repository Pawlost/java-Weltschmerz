package com.ritualsoftheold.weltschmerz.landmass.algorithms;

public class Lloyd{
    public void Calculate(double[] x, double y, int sum){
        // double A = 1/2 * sum(x[i]*y[i+1] - x[i+1]*y[i]);
        // Cx = 1/(6*A) * sum((x[i] + x[i+1]) * (x[i]*y[i+1] - x[i+1]*y[i]);
        //  Cy = 1/(6*A) * sum((y[i] + y[i+1]) * (x[i]*y[i+1] - x[i+1]*y[i]);
    }
}